import { check, checkSync, format, formatSync, Formatter } from "./src/fmt.ts";

export { check, checkSync, format, formatSync, Formatter };

export default { format, formatSync, check, checkSync, Formatter };

export { default as defaultOptions, Options } from "./src/options.ts";
