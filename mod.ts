import { check, checkSync, format, formatSync, Formatter } from "./src/fmt.ts";
import { Options } from "./src/options.ts";

export { check, checkSync, format, formatSync, Formatter, Options };

export default { format, formatSync, check, checkSync, Formatter, Options };

export { default as defaultOptions } from "./src/options.ts";
